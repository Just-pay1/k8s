apiVersion: apps/v1
kind: Deployment
metadata:
  name: justpayui
  namespace: justpay
  labels:
    project: justpay
spec:
  replicas: 2
  selector:
    matchLabels:
      app: justpayui
  template:
    metadata:
      labels:
        app: justpayui
        project: justpay
    spec:
      containers:
        - name: justpayui
          image: justpayacrAKS1.azurecr.io/justpayui:latest
          env:
            - name: NEXT_PUBLIC_BACKEND_SERVER
              value: "http://crm:5000"
          ports:
            - containerPort: 3000
      # imagePullSecrets:
      #   - name: acr-auth  # Only needed for non-AKS clusters
