apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing
  namespace: justpay
  labels:
    project: justpay
spec:
  replicas: 1
  selector:
    matchLabels:
      app: billing
  template:
    metadata:
      labels:
        app: billing
        project: justpay
    spec:
      containers:
        - name: billing
          image: justpayACR1.azurecr.io/billing:latest
          ports:
            - containerPort: 3001
#      imagePullSecrets:
#        - name: macr-auth  # Only needed for non-AKS clusters
---
